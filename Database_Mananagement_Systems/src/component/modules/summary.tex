\section{Summary}
%% ═════════════════════════════════════════════════════════════════════════════

\begin{frame}{\textcolor{brown}{Summary}}
  \begin{columns}[T]
    \column{0.50\textwidth}
      \textbf{Concepts Covered}
      \begin{itemize}
        \item What a database is and why it beats a spreadsheet
        \item RDBMS (SQL) vs NoSQL — and when each fits
        \item SQL as the standard interface to relational data
        \item CRUD — the four core data operations
        \item DDL vs DML — structure vs data
        \item Installing PostgreSQL on Windows and Linux
      \end{itemize}

    \column{0.46\textwidth}
      \textbf{Key Commands}
      \begin{itemize}
        \item \texttt{CREATE TABLE} — define structure
        \item \texttt{ALTER TABLE} — modify structure
        \item \texttt{INSERT INTO} — add data
        \item \texttt{SELECT ... WHERE} — query data
        \item \texttt{UPDATE ... SET} — modify data
        \item \texttt{DELETE FROM} — remove data
      \end{itemize}

      \begin{block}{Explore Next}
        \texttt{JOIN}s, foreign keys, indexes, transactions, user roles, and pgAdmin.
      \end{block}
  \end{columns}
\end{frame}
